project PR03 is

   for Main use ("input_output.ads", "input_output.ads", "matrice_creuse.ads","matrice_creuse.adb","matrice_pleine.ads","matrice_pleine.adb","page_rank.adb","test.adb");

   package Builder is
      for Default_Switches ("ada") use ("-s");
   end Builder;

   package Compiler is
      for Default_Switches ("ada") use ("-gnatwa", "-gnata", "-g");
   end Compiler;

   package Binder is
      for Default_Switches ("ada") use ("-E");
   end Binder;

end PR03;

